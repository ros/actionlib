Index: CMakeLists.txt
===================================================================
--- CMakeLists.txt	(revision 265)
+++ CMakeLists.txt	(working copy)
@@ -41,7 +41,7 @@
 file(GLOB sources src/*.cpp)
 
 include_directories(${YAML_CPP_SOURCE_DIR}/include)
-add_library(yaml-cpp
+add_library(yaml-cpp SHARED
 	${LIB_TYPE}
 	${public_headers}
 	${private_headers}
